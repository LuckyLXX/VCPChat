# -------------------------------------------------------------------
# PandocConverter 插件配置文件
# -------------------------------------------------------------------
# 复制此文件为 config.env 并根据需要修改配置

# -------------------------------------------------------------------
# [Pandoc 基础配置]
# -------------------------------------------------------------------
# Pandoc 可执行文件路径
# 如果 Pandoc 在系统 PATH 中，可以使用 "pandoc"
# 否则需要指定完整路径，如：/usr/local/bin/pandoc 或 C:\Program Files\Pandoc\pandoc.exe
PANDOC_PATH=pandoc

# 输出文件目录
# 转换后的文件将保存在此目录
OUTPUT_DIR=./outputs

# 临时文件目录
# 处理过程中的临时文件将保存在此目录
TEMP_DIR=./temp

# -------------------------------------------------------------------
# [功能开关配置]
# -------------------------------------------------------------------
# 是否启用 MathJax 数学公式支持
# true: 启用数学公式渲染
# false: 禁用数学公式渲染
ENABLE_MATHJAX=true

# 是否启用代码语法高亮
# true: 启用代码高亮
# false: 禁用代码高亮
ENABLE_SYNTAX_HIGHLIGHTING=true

# 是否自动清理临时文件
# true: 转换完成后自动清理临时文件
# false: 保留临时文件用于调试
CLEANUP_TEMP_FILES=true

# -------------------------------------------------------------------
# [PDF 生成配置]
# -------------------------------------------------------------------
# 默认 PDF 生成引擎
# 可选值: pdflatex, xelatex, lualatex
# xelatex: 支持中文和 Unicode 字符（推荐）
# pdflatex: 传统 LaTeX 引擎
# lualatex: 基于 Lua 的 LaTeX 引擎
DEFAULT_PDF_ENGINE=xelatex

# -------------------------------------------------------------------
# [性能和限制配置]
# -------------------------------------------------------------------
# 最大文件大小限制（MB）
# 超过此大小的文件将被拒绝处理
MAX_FILE_SIZE_MB=50

# 转换超时时间（秒）
# 单个文件转换的最大时间限制
CONVERSION_TIMEOUT=300

# -------------------------------------------------------------------
# [默认样式配置]
# -------------------------------------------------------------------
# 默认代码高亮样式
# 可选值: pygments, kate, monochrome, breezedark, espresso, haddock, tango, zenburn
DEFAULT_HIGHLIGHT_STYLE=pygments

# 默认数学公式渲染方法
# 可选值: mathjax, katex, webtex, mathml, latex
DEFAULT_MATH_METHOD=webtex

# -------------------------------------------------------------------
# [高级配置]
# -------------------------------------------------------------------
# 是否启用调试模式
# true: 输出详细日志信息
# false: 只输出基本日志信息
DEBUG_MODE=false

# 是否保留原始 HTML
# true: 在 Markdown 中保留原始 HTML 标签
# false: 过滤 HTML 标签
PRESERVE_RAW_HTML=true

# 是否保留制表符
# true: 保留文档中的制表符
# false: 将制表符转换为空格
PRESERVE_TABS=false

# 默认制表符宽度（当 PRESERVE_TABS=true 时有效）
DEFAULT_TAB_STOP=4

# -------------------------------------------------------------------
# [网络配置]
# -------------------------------------------------------------------
# 下载文件超时时间（秒）
DOWNLOAD_TIMEOUT=30

# 用户代理字符串
# 用于下载网络文件时的 User-Agent
USER_AGENT=PandocConverter/1.0

# -------------------------------------------------------------------
# [安全配置]
# -------------------------------------------------------------------
# 允许的文件扩展名（逗号分隔）
# 空表示允许所有扩展名
ALLOWED_EXTENSIONS=

# 禁止的文件扩展名（逗号分隔）
# 出于安全考虑，禁止处理这些类型的文件
FORBIDDEN_EXTENSIONS=exe,bat,cmd,com,scr,msi,dll,so,dylib

# -------------------------------------------------------------------
# [自定义模板配置]
# -------------------------------------------------------------------
# HTML 模板文件路径
# 如果指定，将使用此模板生成 HTML
HTML_TEMPLATE_PATH=

# LaTeX 模板文件路径
# 如果指定，将使用此模板生成 LaTeX/PDF
LATEX_TEMPLATE_PATH=

# 自定义 CSS 文件路径
# 用于 HTML 输出的样式表
CUSTOM_CSS_PATH=

# -------------------------------------------------------------------
# [日志配置]
# -------------------------------------------------------------------
# 日志级别
# 可选值: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 日志文件路径
# 留空表示输出到控制台
LOG_FILE=

# -------------------------------------------------------------------
# [示例配置]
# -------------------------------------------------------------------
# 以下是一些常用场景的配置示例，可以根据需要取消注释并修改

# # 中文文档优化配置
# DEFAULT_PDF_ENGINE=xelatex
# DEFAULT_MATH_METHOD=webtex
# PRESERVE_RAW_HTML=true
# ENABLE_MATHJAX=true

# # 技术文档配置
# DEFAULT_HIGHLIGHT_STYLE=github
# PRESERVE_TABS=true
# DEFAULT_TAB_STOP=4
# ENABLE_SYNTAX_HIGHLIGHTING=true

# # 学术论文配置
# DEFAULT_PDF_ENGINE=xelatex
# DEFAULT_MATH_METHOD=mathml
# LATEX_TEMPLATE_PATH=./templates/academic.tex